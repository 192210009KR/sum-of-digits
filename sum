#include<stdio.h>
int main()
{
	int number,remainder,sum;
	printf("enter a number:");
	scanf("%d",&number);
	
	while (number>0){
	
		remainder=number % 10;
		sum=sum + remainder;
		number = number / 10;
}
	printf("sum of the digits= %d",sum);
	return 0;
	
}
